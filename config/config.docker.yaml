server:
  port: "8080"

monitor:
  port: "8081"

upstream:
  base_url: "https://api.openai.com"
  api_key: "sk-placeholder"

debug:
  output_dir: "/app/data/traces"
  mask_key: true

chaos:
  enabled: false
  rules: []
