name: Go CI

on:
  push:
    branches: [ "main" ]
    tags:
      - "v*"
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Bun
      uses: oven-sh/setup-bun@v2
      with:
        bun-version: 1.3.11

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.25.x'
        cache: true

    - name: Install Go dependencies
      run: go mod download

    - name: Install UI dependencies
      working-directory: web/monitor-ui
      run: bun install --frozen-lockfile

    - name: Build Trace Monitor UI
      working-directory: web/monitor-ui
      run: bun run build

    - name: Build
      run: go build -v -o llm-tracelab ./cmd/server

    - name: Test
      run: go test -v ./...

    - name: Upload Binary
      uses: actions/upload-artifact@v4
      with:
        name: llm-tracelab-linux-amd64
        path: llm-tracelab

  docker:
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'push'
    steps:
    - uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    # 自动生成 tags（latest / sha / version tag）
    - name: Docker metadata
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: ${{ secrets.DOCKERHUB_USERNAME }}/llm-tracelab
        tags: |
          type=raw,value=latest,enable={{is_default_branch}}
          type=sha
          type=ref,event=tag

    - name: Build and push multi-arch
      uses: docker/build-push-action@v6
      with:
        context: .
        platforms: linux/amd64,linux/arm64
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
        cache-from: type=gha
        cache-to: type=gha,mode=max
